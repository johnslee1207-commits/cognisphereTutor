## 2026-07-25 — 去掉 Tutor 侧全域硬编码（leetcode/ap/aws）

| 字段 | 内容 |
|------|------|
| **阶段** | 域无关化落地（产品代码） |
| **用户问** | 除 leetcode 外，aws / ap_calculus 是否也混入 Tutor？ |
| **答案** | **是（已有，现已清理）**：`learning_loop_mapping.domain_expected_knowledge_keys` 曾写死 leetcode / ap_calculus / aws_certification；`plugin_importer` 有 LeetCode 形 fallback 与 `calculus_graph` 偏置；其余默认回退主要为 leetcode |
| **动作** | 去掉 `default_domain`；adapters 改 `module_template`+`module_key`；缺 domain → `domain_required`；统一 `import_benchmark_case`；env → `COGNISPHERE_SANDBOX_AUTHORIZED` / `COGNISPHERE_ARTIFACT_DIR`（legacy 兼容一期）；expected keys 插件声明或通用；CLI/API 强制 `--domain`/`domain`；FE 从 `csphere-{domain}` 解析；README 双语文档 |
| **测试** | `tests/integrations/test_cognisphere_registry.py` + `tests/learning/test_cognisphere_seed.py` → **26 passed** |
| **状态** | 完成；**未 commit** |
| **阻断** | 插件 manifest 尚未普遍声明 `runtime_modules` 时，仍靠 `cognisphere_plugins.{domain}.{module_key}` 约定；真实插件仓需对齐该约定或补 manifest |

---

## 2026-07-25 — LeetCode 默认配置气味盘点（只读）

| 字段 | 内容 |
|------|------|
| **阶段** | 对照架构共识：为何 Tutor 内有 leetcode 默认/硬编码 |
| **结论** | 属 DT-P4/P5 MVP「首个可跑域」捷径（commit `27e8675`/`2d17d94`），非产品意图把 LeetCode 做成 Tutor 内置域；FE 已动态发现插件，无硬编码按钮 |
| **必须迁出/去掉** | `default_domain`/`"leetcode"` 回退、`import_benchmark_case` 域分支、`runtime_adapters` 仅挂 leetcode 模块、`COGNISPHERE_LEETCODE_*` 命名、`domain_expected_knowledge_keys` 写死在 Tutor、API 文案 LC-003 |
| **可保留** | 测试 fixture、CLI/README 示例用词、发现列表驱动的 FE 域按钮 |
| **状态** | 分析完成；未改业务代码、未 commit |

---

## 2026-07-25 — 架构共识落档（Plugin 域逻辑 / Tutor 通用编排）

| 字段 | 内容 |
|------|------|
| **阶段** | 架构共识显式化（文档） |
| **共识** | 领域学习特定功能通过 Cognisphere Learning Plugins 实现；Tutor 侧做通用编排/协商/教学闭环，不把域专属逻辑写进核心 |
| **动作** | 在 `README.md`、`assets/README/README_CN.md` Cognisphere 节与 `AGENTS.md`（Architecture + Key Files）写入边界；修正 `integrations/__init__.py` 过时 stub 说明 |
| **对照** | 与实现规范/用户手册边界及现有 DT-P1…P6（`integrations/cognisphere/` + Learning Space / mastery_path）一致；手册 `planner/tutor/...` 目录差异为有意分歧 |
| **气味** | `default_domain=leetcode`、`import_benchmark_case` 的 leetcode 分支、`COGNISPHERE_LEETCODE_*` 环境变量名、`runtime_adapters.json` 目前仅挂 leetcode 模块——属适配层默认/命名债，非核心域逻辑；后续宜数据驱动化、去掉硬编码分支 |
| **状态** | 文档落档完成；未 commit |

---

## 2026-07-25 — Cognisphere 用户手册 + 实现规范 v1.0 对照缺口评估

| 字段 | 内容 |
|------|------|
| **阶段** | 文档对照分析（只读，未改业务代码） |
| **输入** | `Cognisphere_Learning_System_User_Manual_v1.0.md`；`Cognisphere_Learning_System_Implementation_Specification_v1.0.md` |
| **结论** | 两份手册为高层愿景/边界文档，不含 DT-P1…P6 级 API 细节；代码侧集成层（`integrations/cognisphere`）与手册架构边界对齐度高，但用户手册描述的「目标驱动端到端学习体验」产品化缺口大 |
| **状态** | 分析完成；未 commit |
| **下一步** | 按评估 backlog 执行 P0（NL 目标→插件协商 UI、能力雷达、闭环自动化）或先补 README 愿景↔实现对照说明 |

---

## 2026-07-24 — P1 缺口落地（Multi_user / Config·RAG / 共享枢纽）

| 字段 | 内容 |
|------|------|
| **阶段** | 按建议执行 P1 |
| **动作** | 隔离测试扩展 learning/skills/KB；`load_pageindex_settings` 门面；agentic PageIndex 走 `get_pipeline`；`tests/agents/_shared` 枢纽门禁单测；AGENTS.md Key Files 纪律；knowledge `_kb_base_dir` 标 deprecated；Windows partners `as_posix()` 断言；README/README_CN 多用户隔离边界说明 |
| **测试** | `tests/multi_user/test_resource_isolation.py` + `tests/agents/_shared/` → **8 passed** |
| **状态** | P1 最小落地完成；未 commit |
| **下一步** | P2：math_animator 友好降级；Tools Coming soon 清理；Sandbox 注册表校验；或 persona_access 抽离 |

---

| 字段 | 内容 |
|------|------|
| **阶段** | 按建议执行 P0 |
| **动作** | DT-P3：`trusted_context_status` / `fetch_and_import` / API `trusted-context/import` + status.gates；DT-P6：interview/benchmark 不可用时抛 `benchmark_unavailable`（禁静默 stub）；Mastery：`?capability=mastery_path` 深链 + tutor SSE `tutor/events` + EventBus `COGNISPHERE_TUTOR`；README 双路径说明 |
| **测试** | `tests/integrations/test_cognisphere_registry.py` + `tests/learning/test_cognisphere_seed.py` 通过 |
| **状态** | P0 代码完成；未 commit |
| **下一步** | P1：Multi_user 边界 / Config·RAG 碎片 / 共享枢纽改动纪律 |

---

| 字段 | 内容 |
|------|------|
| **阶段** | 架构缺口排期 + Capability 调用链分析 |
| **动作** | GitNexus CALLS/impact；对照 7 个 Turn Capability + 5 个 Loop Capability；产出 Canvas `gap-priority-and-callchains.canvas.tsx` |
| **P0** | DT-P3 在线 trusted-context；DT-P6 stub→实联；Mastery 自动切模式 + tutor WS |
| **P1** | Multi_user 内聚；Config/RAG 碎片；`emit_capability_result`(CRITICAL) / `compose_enabled_tools`(HIGH) 改动纪律 |
| **调用链结论** | 模式 A：chat/solve/mastery 复用 AgenticChatPipeline+LOOP hooks；模式 B：research/question/visualize/math_animator 独立 Pipeline + 统一 emit |
| **状态** | 分析完成；未改业务代码 |
| **下一步** | 按 P0 开工实现，或指定某一能力做更细到函数级的逐步 trace |

---

| 字段 | 内容 |
|------|------|
| **阶段** | 代码图谱构建与架构功能实现成熟度分析 |
| **动作** | `npx gitnexus analyze`（索引名 DeepTutor）；41,554 nodes / 78,247 edges / 1,148 clusters / 300 flows；Cypher 聚合社区/继承图；产出 Canvas `architecture-maturity.canvas.tsx` |
| **状态** | 图谱已就绪且与 HEAD 同步；综合成熟度均值约 **74/100** |
| **结论摘要** | 核心 Agent Runtime / Capabilities / Services / Partners 已达生产成熟；Cognisphere DT-P1–P5 集成 MVP 可用，P6 仍有 stub；Learning/Mastery 刚产品绑定；Multi_user 内聚最低 |
| **下一步** | 打通 P3 在线 trusted-context；去掉 P6 stub；巩固 mastery 产品最后一公里；治理 Multi_user/Config 碎片 |

---

| 字段 | 内容 |
|------|------|
| **阶段** | Mastery Path / Learning Space 产品绑定 |
| **动作** | `learning/cognisphere_seed.py`；API `/api/v1/learning/cognisphere/*`（status / import-and-seed / tutor/start / suggest-focus / plan-path）；Learning Space 侧栏导入域包 → `csphere-{domain}` 路径；苏格拉底练习入口；测试 23 passed |
| **状态** | 已提交：`2d17d94`（UI/API）、`d1d49e1`（README_CN）；**未 push** |
| **下一步** | 可选：Chat 会话自动切换 mastery_path 模式；WebSocket 推送 tutor 事件 |

## 2026-07-24 — DT-P4/P5/P6 产品绑定加深（runtime bridge）

| 字段 | 内容 |
|------|------|
| **阶段** | 对接规范 §7.6 / DT-P4–P6 加深：回调落盘 → 驱动插件离线 runtime |
| **动作** | 新增 `runtime_adapters.json` + `runtime_bridge.py`（Socratic / Sandbox / Mistake / Skill Graph / Interview）；CLI `tutor-start`/`tutor-advance`/`sandbox-verify`/`suggest-focus`/`plan-path`/`interview`；`import_benchmark_case` 优先走 P5 expert_benchmark_runtime；README/AGENTS/数据层 registry 更新；测试 20 passed；真实插件仓 smoke 全绿 |
| **状态** | 已纳入 commit `27e8675`（与更名一并提交） |
| **阻断** | DT-P3 在线 kit 仍需 `COGNISPHERE_TRUSTED_CONTEXT_BASE_URL`；Live LLM tutor/interviewer 仍 fail-closed；LeetCode export problems 非空仍依赖宿主 LC-003 |
| **下一步** | Guided Learning UI 绑定（见上条） |

## 2026-07-24 — Cognisphere 集成 DT-P2…P6 + 产品更名 cognisphereTutor

| 字段 | 内容 |
|------|------|
| **阶段** | DT-P2 Bundle Import；DT-P3 Trusted Context；DT-P4/P5 Runtime Callbacks；DT-P6 Cross-domain；产品更名/目录迁移 |
| **动作** | 实现 `export_and_import` / learning_loop→Assessment·Plan·Mastery 映射与导入回执；CLI `import`/`import-bundle`/`compose`/`cross-domain`；trusted-context 离线导入 + 在线 fail-closed；tutor/sandbox/mistake/mastery 回调落盘；跨域 compose；契约/数据层 manifests；测试 18 passed；真实插件仓 smoke（leetcode 可导入，problems 空因可解释：LC-003 缺失走 skeleton） |
| **状态** | 集成 MVP 本地完成；更名迁移见同日后续条目；**未 commit、未 push** |
| **阻断** | DT-P3 在线 kit 需 `COGNISPHERE_TRUSTED_CONTEXT_BASE_URL`；DT-P6 benchmark/interview 插件面未暴露时 stub；LeetCode 完整 knowledge 依赖宿主 LC-003 ontology |
| **下一步** | 用户确认后更换 GitHub remote → 再 commit；配置 trusted-context kit；补齐 LC-003 后复验非空 problems |

## 2026-07-24 — cognisphereTutor × Cognisphere Plugin Integration（DT-P1）

| 字段 | 内容 |
|------|------|
| **阶段** | 设计评估 + DT-P1 实现（Registry / Negotiate / Validate） |
| **动作** | 阅读 v1.0 对接规范；对照 Tools/Capabilities 模型评估；实现 `cognispheretutor/integrations/cognisphere/`；CLI `cognispheretutor cognisphere`；契约 fixture 测试；README/AGENTS 环境变量说明；数据层 registry |
| **状态** | DT-P1 完成（本地实现，未 commit、未 push） |
| **下一步** | DT-P2 Bundle Import 映射学习闭环；若需提交：先更换 GitHub remote，再由用户明确要求 commit/push |

### 评估摘要
- 规范与 cognisphereTutor 产品边界清晰；Cognisphere/插件侧 handoff 已就绪，cognisphereTutor 侧此前无对等集成层。
- DT-P1 不依赖改动 Level-1 Tools / Level-2 Capabilities 核心编排；集成作为旁路 client 更合适。
- 主要风险：live sandbox/GraphQL/LLM 闸门、bundle safety、Host HTTP 路径与 trusted-context（DT-P3）尚未接线。

## 2026-07-24 — 产品更名 DeepTutor → cognisphereTutor（目录迁移）

| 字段 | 内容 |
|------|------|
| **阶段** | 品牌/包名/目录迁移 |
| **动作** | 包 `deeptutor`→`cognispheretutor`，`deeptutor_cli`→`cognispheretutor_cli`，`deeptutor_web`→`cognispheretutor_web`；pyproject/CLI `cognispheretutor`（保留 `deeptutor` 兼容别名）；文案 DeepTutor→cognisphereTutor；Cognisphere 合约字段（`deeptutor_entrypoint` / `export_deeptutor_package` 等）保留；因 Cursor 锁定无法直接 Rename 原目录，已 robocopy 至 `D:\Projects\cognisphereTutor` |
| **状态** | 新根路径可用；原 `D:\Projects\DeepTutor` 仍被占用，迁移 agent 根后可手动删除旧目录 |
| **确认** | **未 commit、未 push、未改 remote** |
| **下一步** | `move_agent_to_root` → 在新根验证测试 → 用户换 remote 后自行 commit |


## 2026-07-24 — 产品更名 DeepTutor → cognisphereTutor（目录迁移）

| 字段 | 内容 |
|------|------|
| **阶段** | 品牌/包名/目录迁移 |
| **动作** | 包 `deeptutor`→`cognispheretutor`，`deeptutor_cli`→`cognispheretutor_cli`，`deeptutor_web`→`cognispheretutor_web`；pyproject/CLI `cognispheretutor`（保留 `deeptutor` 兼容别名）；文案 DeepTutor→cognisphereTutor；Cognisphere 合约字段（`deeptutor_entrypoint` / `export_deeptutor_package` 等）保留；因 Cursor 锁定无法直接 Rename 原目录，已 robocopy 至 `D:\Projects\cognisphereTutor` |
| **状态** | 新根路径可用；原 `D:\Projects\DeepTutor` 仍被占用，迁移 agent 根后可手动删除旧目录 |
| **确认** | **未 commit、未 push、未改 remote** |
| **下一步** | `move_agent_to_root` → 在新根验证测试 → 用户换 remote 后自行 commit |

